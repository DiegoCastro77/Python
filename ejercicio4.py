"""
#FOR

for variable in elemento_iterable (lista,rango, etc)
    BLOQUE DE INSTRUCCIONES
"""

contador = 0 #iniciamos el contador en cero
for contador in range(0,5): #Rango iterable
    print("Voy por el " +str(contador)) #Str para pasarlo a string y luego concatenarlo con el print 
#Contador es la variable que almacena hasta que se termina el rango    